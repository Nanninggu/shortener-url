<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hlink.urlshortener.mapper.TeamMemberMapper">

    <resultMap id="TeamMemberResultMap" type="com.hlink.urlshortener.model.TeamMember">
        <id property="id" column="id"/>
        <result property="teamId" column="team_id"/>
        <result property="userId" column="user_id"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.hlink.urlshortener.model.TeamMember" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO team_members (team_id, user_id, role, created_at)
        VALUES (#{teamId}, #{userId}, #{role}, #{createdAt})
    </insert>

    <select id="findByTeamIdAndUserId" resultMap="TeamMemberResultMap">
        SELECT * FROM team_members WHERE team_id = #{teamId} AND user_id = #{userId}
    </select>

    <select id="findByTeamId" resultMap="TeamMemberResultMap">
        SELECT * FROM team_members WHERE team_id = #{teamId} ORDER BY created_at DESC
    </select>

    <delete id="deleteByTeamIdAndUserId">
        DELETE FROM team_members WHERE team_id = #{teamId} AND user_id = #{userId}
    </delete>

    <delete id="deleteByTeamId">
        DELETE FROM team_members WHERE team_id = #{teamId}
    </delete>

</mapper>

